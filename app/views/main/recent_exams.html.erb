
<table class="table table-striped">
  <% @studies.each do |study| %>
    <% entry = study["resource"] %>
    <tr class="hoverable">
      <td><%= (entry["accession"] || {})["value"] %></td>
      <td><%= ((entry["patient"] || {})["reference"] || "").split("/").last %></td>
      <td><%= entry["started"] %></td>
      <td><%= entry["description"] %></td>
    </tr>
  <% end %>
</table>
